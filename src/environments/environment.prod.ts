export const environment = {
  production: true,
  apiUrl: 'https://your-api-domain.com',
  platformDomain: 'your-platform-domain.com',
  supportedLanguages: ['fa', 'en'],
  defaultLanguage: 'fa',
  storeDomains: {
    // Production store domains will be configured here
  },
  features: {
    enableAnalytics: true,
    enablePWA: true,
    enableOffline: true
  }
};
